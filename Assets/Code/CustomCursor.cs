using System.Collections;
//-583.2  -327.4
using System.Collections.Generic;
using UnityEngine;

public class CustomCursor : MonoBehaviour
{
    // public Texture2D cursorTexture; // Assign your cursor sprite here
    // public float cursorSize = 1.0f; // Cursor size factor
    //
    // void Start()
    // {
    //     // Set the custom cursor with size
    //     Cursor.SetCursor(cursorTexture, Vector2.zero, CursorMode.Auto);
    //     // Adjust cursor size
    //     Cursor.visible = false; // Hide default cursor
    //     Cursor.lockState = CursorLockMode.Confined; // Keep the cursor confined to the game window
    //     Vector2 cursorHotspot = new Vector2(cursorTexture.width * 0.5f, cursorTexture.height * 0.5f);
    //     Cursor.SetCursor(cursorTexture, cursorHotspot, CursorMode.Auto);
    // }
    //
    // void Update()
    // {
    //     // Adjust cursor size if changed during runtime
    //     Vector2 cursorHotspot = new Vector2(cursorTexture.width * 0.5f * cursorSize, cursorTexture.height * 0.5f * cursorSize);
    //     Cursor.SetCursor(cursorTexture, cursorHotspot, CursorMode.Auto);
    // }
}

